# Vantelyx CLM client env example (copy to client/.env to enable API mode).
# Leave VITE_API_BASE_URL unset/blank to run in frontend-only localStorage demo mode.
# When set, the app calls the API first and falls back to localStorage if it is unreachable.
VITE_API_BASE_URL=http://localhost:5058
